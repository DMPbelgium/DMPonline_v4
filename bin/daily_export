#!/bin/bash
readonly appdir=$(cd `dirname $0`/.. && pwd)

dir=$1

if [ ! -d "$dir" ];then
    echo "$dir is not a directory" >&2
    exit 1
fi

cd $appdir

./bin/daily_export_projects $dir &&
./bin/daily_export_updated_projects $dir &&
./bin/daily_export_deleted_projects $dir
