<p>Hello <%= @project_group.user.name(true) %></p>

<% if !(@project_group.user.confirmed?) %>

  <% project_title = @project_group.project.title %>

<% else %>

  <% project_title = link_to(@project_group.project.title, project_path(@project_group.project, :only_path => false)) %>

<% end %>

<p><%= @changed_by.reference %> has changed your permissions relating to the DMP <b>"<%= project_title %>"</b>. Your access level is now <b>"<%= @project_group.t_access_level %>"</b>.</p>

<% if !(@project_group.user.confirmed?) %>

  <p>
    <%= link_to 'Sign in', confirmation_url( @project_group.user, :confirmation_token => @project_group.user.confirmation_token) %>
  </p>

<% end %>
